<!DOCTYPE html>
<html lang="pt">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>GSL | RL TEAM — Esports Organization</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Rajdhani:wght@400;500;600;700&family=Bebas+Neue&family=Exo+2:wght@300;400;600;800&display=swap" rel="stylesheet">
<style>
  :root {
    --blue: #1a6ecc;
    --blue-bright: #4db8ff;
    --blue-mid: #2d8cf0;
    --blue-dark: #0a2a5e;
    --blue-deeper: #051430;
    --blue-glow: rgba(77,184,255,0.45);
    --blue-glow-soft: rgba(45,140,240,0.25);
    --black: #040810;
    --dark: #060d1a;
    --dark2: #0a1526;
    --dark3: #0f1e35;
    --navy: #0d1b2e;
    --white: #ffffff;
    --grey: #7a9bbf;
    --light-grey: #b0d0f0;
    --wave-cyan: #ff3fa4;
    --pink-glow: rgba(255,63,164,0.4);
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }
  html { scroll-behavior: smooth; }
  body { font-family: 'Exo 2', sans-serif; background: var(--dark); color: var(--white); overflow-x: hidden; cursor: none; }

  #cursor { width: 12px; height: 12px; background: var(--blue-bright); border-radius: 50%; position: fixed; pointer-events: none; z-index: 99999; transition: transform 0.1s; }
  #cursor-ring { width: 36px; height: 36px; border: 1.5px solid var(--blue-bright); border-radius: 50%; position: fixed; pointer-events: none; z-index: 99998; transition: transform 0.15s, opacity 0.3s; opacity: 0.5; }

  body::before { content: ''; position: fixed; inset: 0; background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E"); pointer-events: none; z-index: 9999; opacity: 0.25; }

  nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 1000;
    background: rgba(4,8,16,0.96); backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(77,184,255,0.18);
    padding: 0 5%; display: flex; align-items: center; justify-content: space-between; height: 70px;
  }
  .logo {
    font-family: 'Bebas Neue', sans-serif; font-size: 1.6rem; letter-spacing: 3px;
    color: var(--white); text-decoration: none; display: flex; align-items: center; gap: 14px;
  }
  .logo-shield {
    width: 44px; height: 44px; border-radius: 8px; object-fit: cover;
    border: 2px solid rgba(77,184,255,0.5); box-shadow: 0 0 16px var(--blue-glow);
  }
  .logo span { color: var(--blue-bright); }
  .nav-links { display: flex; gap: 38px; list-style: none; align-items: center; }
  .nav-links a {
    text-decoration: none; color: var(--light-grey); font-family: 'Rajdhani', sans-serif;
    font-size: 0.95rem; font-weight: 600; letter-spacing: 2px; text-transform: uppercase;
    transition: color 0.3s; position: relative;
  }
  .nav-links a::after {
    content: ''; position: absolute; bottom: -4px; left: 0; width: 0; height: 2px;
    background: var(--blue-bright); transition: width 0.3s;
  }
  .nav-links a:hover { color: var(--white); }
  .nav-links a:hover::after { width: 100%; }
  .nav-links a.active { color: var(--blue-bright); }
  .nav-links a.active::after { width: 100%; }
  .nav-admin-btn {
    background: transparent; border: 1px solid rgba(77,184,255,0.3); color: var(--grey);
    padding: 6px 16px; font-family: 'Rajdhani', sans-serif; font-size: 0.75rem;
    letter-spacing: 2px; text-transform: uppercase; cursor: pointer; transition: all 0.3s; border-radius: 2px;
  }
  .nav-admin-btn:hover { border-color: var(--blue-bright); color: var(--blue-bright); }

  .page { display: none; min-height: 100vh; }
  .page.active { display: block; }

  /* HERO */
  .hero {
    min-height: 100vh; display: flex; flex-direction: column;
    align-items: center; justify-content: center;
    position: relative; overflow: hidden; padding-top: 70px;
  }
  .hero-bg {
    position: absolute; inset: 0;
    background: radial-gradient(ellipse 90% 70% at 50% 35%, rgba(13,27,46,0.95) 0%, var(--dark) 100%);
  }

  .hero-glow-pulse {
    position: absolute; inset: 0; overflow: hidden; pointer-events: none;
    background: radial-gradient(circle at 50% 20%, var(--pink-glow) 0%, transparent 55%);
    animation: glowPulse 6s ease-in-out infinite;
  }
  @keyframes glowPulse { 0%,100% { opacity: 0.5; } 50% { opacity: 1; } }

  .hero-grid {
    position: absolute; inset: 0;
    background-image: linear-gradient(rgba(77,184,255,0.05) 1px, transparent 1px),
                      linear-gradient(90deg, rgba(77,184,255,0.05) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse 80% 80% at 50% 50%, black 0%, transparent 100%);
  }

  /* Orbs */
  .hero-orbs { position: absolute; inset: 0; overflow: hidden; pointer-events: none; }
  .orb {
    position: absolute; border-radius: 50%;
    background: radial-gradient(circle, rgba(77,184,255,0.18) 0%, transparent 70%);
    animation: floatOrb 9s ease-in-out infinite;
  }
  .orb:nth-child(1) { width: 400px; height: 400px; top: 5%; left: -5%; animation-delay: 0s; }
  .orb:nth-child(2) { width: 250px; height: 250px; top: 55%; right: 5%; animation-delay: 3s; }
  .orb:nth-child(3) { width: 180px; height: 180px; top: 25%; right: 20%; animation-delay: 6s; background: radial-gradient(circle, rgba(255,63,164,0.14) 0%, transparent 70%); }
  @keyframes floatOrb { 0%,100% { transform: translateY(0) scale(1); opacity: 0.6; } 50% { transform: translateY(-25px) scale(1.08); opacity: 1; } }

  .hero-content { position: relative; z-index: 2; text-align: center; padding: 0 20px; }

  /* ARL Shield display */
  .hero-shield-wrap { margin-bottom: 32px; opacity: 0; animation: fadeUp 0.8s 0.1s forwards; }
  .hero-shield {
    width: 160px; height: 160px; border-radius: 16px; object-fit: cover;
    border: 2px solid rgba(77,184,255,0.4);
    box-shadow: 0 0 50px var(--blue-glow), 0 0 100px rgba(26,110,204,0.25);
    animation: pulseShield 3.5s ease-in-out infinite;
  }
  @keyframes pulseShield {
    0%,100% { box-shadow: 0 0 50px var(--blue-glow), 0 0 100px rgba(26,110,204,0.25); }
    50% { box-shadow: 0 0 70px rgba(77,184,255,0.9), 0 0 140px rgba(26,110,204,0.5); }
  }

  .hero-tag {
    display: inline-block; font-family: 'Rajdhani', sans-serif; font-size: 0.82rem;
    letter-spacing: 6px; color: var(--wave-cyan); text-transform: uppercase;
    margin-bottom: 18px; opacity: 0; animation: fadeUp 0.8s 0.3s forwards;
  }
  .hero-title {
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(3.5rem, 11vw, 9rem); line-height: 0.9; letter-spacing: 6px;
    opacity: 0; animation: fadeUp 0.8s 0.5s forwards; color: var(--white);
  }
  .hero-title .blue { color: var(--blue-bright); text-shadow: 0 0 50px var(--blue-glow); }
  .hero-subtitle {
    font-size: clamp(0.9rem, 2.5vw, 1.2rem); color: var(--grey); font-weight: 300;
    letter-spacing: 4px; text-transform: uppercase; margin-top: 20px;
    opacity: 0; animation: fadeUp 0.8s 0.7s forwards; max-width: 600px; margin-left: auto; margin-right: auto;
  }
  .hero-divider {
    width: 140px; height: 2px;
    background: linear-gradient(90deg, transparent, var(--wave-cyan), var(--blue-bright), transparent);
    margin: 28px auto; opacity: 0; animation: fadeUp 0.8s 0.8s forwards;
  }
  .hero-desc {
    font-size: 1rem; color: var(--light-grey); max-width: 620px;
    margin: 0 auto 44px; line-height: 1.8; opacity: 0; animation: fadeUp 0.8s 1.0s forwards;
  }
  .hero-btns { display: flex; gap: 20px; justify-content: center; flex-wrap: wrap; opacity: 0; animation: fadeUp 0.8s 1.1s forwards; }

  .btn {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 14px 36px; font-family: 'Rajdhani', sans-serif; font-size: 1rem;
    font-weight: 700; letter-spacing: 3px; text-transform: uppercase;
    border: none; cursor: pointer; text-decoration: none; transition: all 0.3s;
    position: relative; overflow: hidden;
  }
  .btn::before {
    content: ''; position: absolute; inset: 0;
    background: rgba(255,255,255,0.08); transform: translateX(-100%); transition: transform 0.3s;
  }
  .btn:hover::before { transform: translateX(0); }
  .btn-primary {
    background: linear-gradient(135deg, var(--blue) 0%, var(--blue-dark) 100%);
    color: var(--white);
    clip-path: polygon(0 0, calc(100% - 12px) 0, 100% 12px, 100% 100%, 12px 100%, 0 calc(100% - 12px));
  }
  .btn-primary:hover {
    background: linear-gradient(135deg, var(--blue-mid) 0%, var(--blue) 100%);
    box-shadow: 0 0 30px var(--blue-glow); transform: translateY(-2px);
  }
  .btn-outline {
    background: transparent; color: var(--white);
    border: 1px solid rgba(77,184,255,0.35);
    clip-path: polygon(0 0, calc(100% - 12px) 0, 100% 12px, 100% 100%, 12px 100%, 0 calc(100% - 12px));
  }
  .btn-outline:hover { border-color: var(--blue-bright); color: var(--blue-bright); box-shadow: 0 0 20px var(--blue-glow-soft); transform: translateY(-2px); }

  /* SOCIAL */
  .hero-social { margin-top: 40px; text-align: center; opacity: 0; animation: fadeUp 0.8s 1.3s forwards; }
  .hero-social-label { font-family: 'Rajdhani', sans-serif; font-size: 0.75rem; letter-spacing: 5px; color: var(--grey); text-transform: uppercase; margin-bottom: 16px; }
  .hero-social-btns { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; }
  .hero-social-btn {
    display: flex; align-items: center; gap: 12px; padding: 16px 28px;
    text-decoration: none; color: var(--white); border: 1px solid rgba(255,255,255,0.1);
    background: rgba(77,184,255,0.05); backdrop-filter: blur(10px); transition: all 0.3s;
    position: relative; overflow: hidden;
    clip-path: polygon(0 0, calc(100% - 10px) 0, 100% 10px, 100% 100%, 10px 100%, 0 calc(100% - 10px));
  }
  .hero-social-btn::before { content: ''; position: absolute; inset: 0; opacity: 0; transition: opacity 0.3s; }
  .discord-hero-btn::before { background: rgba(88,101,242,0.15); }
  .tiktok-hero-btn::before { background: rgba(77,184,255,0.12); }
  .hero-social-btn:hover::before { opacity: 1; }
  .discord-hero-btn { border-color: rgba(88,101,242,0.35); }
  .discord-hero-btn:hover { border-color: #5865f2; box-shadow: 0 0 30px rgba(88,101,242,0.3), 0 8px 32px rgba(0,0,0,0.4); transform: translateY(-3px); }
  .discord-hero-btn svg { color: #5865f2; position: relative; z-index: 1; }
  .tiktok-hero-btn { border-color: rgba(77,184,255,0.35); }
  .tiktok-hero-btn:hover { border-color: var(--blue-bright); box-shadow: 0 0 30px var(--blue-glow), 0 8px 32px rgba(0,0,0,0.4); transform: translateY(-3px); }
  .tiktok-hero-btn svg { color: var(--blue-bright); position: relative; z-index: 1; }
  .hero-social-btn span:first-of-type { font-family: 'Rajdhani', sans-serif; font-size: 1.1rem; font-weight: 700; letter-spacing: 2px; text-transform: uppercase; position: relative; z-index: 1; }
  .hero-social-handle { font-family: 'Rajdhani', sans-serif; font-size: 0.8rem; letter-spacing: 1px; color: var(--grey); position: relative; z-index: 1; border-left: 1px solid rgba(255,255,255,0.15); padding-left: 12px; }

  section { padding: 100px 5%; }
  .section-tag { font-family: 'Rajdhani', sans-serif; font-size: 0.8rem; letter-spacing: 5px; color: var(--wave-cyan); text-transform: uppercase; margin-bottom: 12px; display: block; }
  .section-title { font-family: 'Bebas Neue', sans-serif; font-size: clamp(2.5rem, 6vw, 5rem); letter-spacing: 4px; line-height: 1; margin-bottom: 20px; }
  .section-desc { color: var(--grey); max-width: 600px; line-height: 1.8; font-size: 1rem; }

  .stats-bar {
    background: var(--dark2);
    border-top: 1px solid rgba(77,184,255,0.2); border-bottom: 1px solid rgba(77,184,255,0.2);
    padding: 60px 5%; display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 40px; text-align: center;
  }
  .stat-number { font-family: 'Bebas Neue', sans-serif; font-size: 3.2rem; color: var(--blue-bright); line-height: 1; text-shadow: 0 0 20px var(--blue-glow-soft); }
  .stat-label { font-family: 'Rajdhani', sans-serif; letter-spacing: 3px; font-size: 0.8rem; text-transform: uppercase; color: var(--grey); margin-top: 8px; }

  .cards-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 2px; margin-top: 60px; }
  .card {
    background: var(--dark2); border: 1px solid rgba(77,184,255,0.1);
    padding: 40px; position: relative; overflow: hidden; transition: all 0.4s; cursor: pointer;
  }
  .card::before {
    content: ''; position: absolute; top: 0; left: 0; width: 100%; height: 3px;
    background: linear-gradient(90deg, var(--blue), var(--wave-cyan));
    transform: scaleX(0); transition: transform 0.4s;
  }
  .card:hover { border-color: rgba(77,184,255,0.35); transform: translateY(-5px); box-shadow: 0 20px 60px rgba(0,0,0,0.5), 0 0 30px var(--pink-glow); }
  .card:hover::before { transform: scaleX(1); }
  .card-icon {
    width: 56px; height: 56px; background: rgba(77,184,255,0.08); border: 1px solid rgba(77,184,255,0.3);
    display: flex; align-items: center; justify-content: center; font-size: 1.5rem; margin-bottom: 24px;
    clip-path: polygon(0 0, calc(100% - 10px) 0, 100% 10px, 100% 100%, 10px 100%, 0 calc(100% - 10px));
  }
  .card h3 { font-family: 'Rajdhani', sans-serif; font-size: 1.3rem; font-weight: 700; letter-spacing: 2px; text-transform: uppercase; margin-bottom: 12px; color: var(--white); }
  .card p { color: var(--grey); line-height: 1.7; font-size: 0.9rem; }

  /* WIN/LOSS */
  .winloss-board { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 20px; margin-bottom: 40px; }
  .wl-box { background: var(--dark2); border: 1px solid rgba(255,255,255,0.06); padding: 36px 24px; text-align: center; transition: transform 0.3s; }
  .wl-box:hover { transform: translateY(-4px); }
  .wl-box.wins { border-top: 3px solid #22c55e; }
  .wl-box.losses { border-top: 3px solid #e05555; }
  .wl-box.ratio { border-top: 3px solid var(--blue-bright); }
  .wl-number { font-family: 'Bebas Neue', sans-serif; font-size: 5rem; line-height: 1; margin-bottom: 8px; }
  .wl-box.wins .wl-number { color: #22c55e; }
  .wl-box.losses .wl-number { color: #e05555; }
  .wl-box.ratio .wl-number { color: var(--blue-bright); }
  .wl-label { font-family: 'Rajdhani', sans-serif; font-size: 0.85rem; letter-spacing: 2px; text-transform: uppercase; color: var(--grey); }
  .wl-bar { height: 6px; background: rgba(255,255,255,0.06); border-radius: 3px; margin-top: 24px; overflow: hidden; }
  .wl-bar-fill { height: 100%; background: linear-gradient(90deg, #22c55e, #16a34a); border-radius: 3px; transition: width 0.8s cubic-bezier(0.4,0,0.2,1); }

  .matches-list { display: flex; flex-direction: column; gap: 8px; }
  .match-row {
    display: grid; grid-template-columns: auto 1fr; align-items: center; gap: 16px;
    background: var(--dark2); border: 1px solid rgba(255,255,255,0.05); padding: 16px 20px; transition: border-color 0.3s;
  }
  .match-row:hover { border-color: rgba(77,184,255,0.2); }
  .match-badge { width: 36px; height: 36px; display: flex; align-items: center; justify-content: center; font-family: 'Bebas Neue', sans-serif; font-size: 1rem; border-radius: 2px; }
  .match-badge.w { background: rgba(34,197,94,0.15); color: #22c55e; border: 1px solid rgba(34,197,94,0.3); }
  .match-badge.l { background: rgba(224,85,85,0.15); color: #e05555; border: 1px solid rgba(224,85,85,0.3); }
  .match-label { font-family: 'Rajdhani', sans-serif; font-weight: 600; font-size: 1rem; letter-spacing: 1px; text-transform: uppercase; color: var(--light-grey); }

  .team-header { display: grid; grid-template-columns: 1fr 1fr; gap: 80px; align-items: center; margin-bottom: 80px; }
  .team-visual {
    position: relative; height: 400px; background: var(--dark2);
    border: 1px solid rgba(77,184,255,0.2); display: flex; align-items: center; justify-content: center; overflow: hidden;
    background: linear-gradient(135deg, var(--dark2), var(--navy));
  }
  .team-visual-text { font-family: 'Bebas Neue', sans-serif; font-size: 8rem; color: rgba(77,184,255,0.05); position: absolute; letter-spacing: 10px; user-select: none; }
  .team-badge {
    width: 160px; height: 160px;
    background: radial-gradient(circle, rgba(77,184,255,0.15), rgba(26,110,204,0.02));
    border: 2px solid rgba(77,184,255,0.4); border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-family: 'Bebas Neue', sans-serif; font-size: 1.8rem; letter-spacing: 2px;
    color: var(--blue-bright); position: relative; z-index: 1;
    box-shadow: 0 0 40px var(--blue-glow-soft);
  }

  /* SOCIAL BIG */
  .social-big-btns { display: flex; gap: 24px; justify-content: center; flex-wrap: wrap; max-width: 900px; margin: 0 auto; }
  .social-big-btn {
    position: relative; display: flex; align-items: center; gap: 24px;
    padding: 28px 40px; text-decoration: none; color: var(--white);
    border: 1px solid rgba(255,255,255,0.08); background: var(--dark2);
    overflow: hidden; transition: all 0.4s; flex: 1; min-width: 300px; max-width: 420px;
    clip-path: polygon(0 0, calc(100% - 16px) 0, 100% 16px, 100% 100%, 16px 100%, 0 calc(100% - 16px));
  }
  .social-big-btn:hover { transform: translateY(-6px); }
  .social-btn-glow { position: absolute; inset: 0; opacity: 0; transition: opacity 0.4s; }
  .discord-glow { background: radial-gradient(ellipse 80% 80% at 30% 50%, rgba(88,101,242,0.25) 0%, transparent 70%); }
  .tiktok-glow { background: radial-gradient(ellipse 80% 80% at 30% 50%, rgba(77,184,255,0.2) 0%, transparent 70%); }
  .social-big-btn:hover .social-btn-glow { opacity: 1; }
  .discord-btn { border-color: rgba(88,101,242,0.3); }
  .discord-btn:hover { border-color: rgba(88,101,242,0.7); box-shadow: 0 20px 60px rgba(88,101,242,0.2); }
  .discord-btn .social-btn-icon { color: #5865f2; background: rgba(88,101,242,0.12); border: 1px solid rgba(88,101,242,0.3); }
  .tiktok-btn { border-color: rgba(77,184,255,0.3); }
  .tiktok-btn:hover { border-color: rgba(77,184,255,0.7); box-shadow: 0 20px 60px rgba(77,184,255,0.2); }
  .tiktok-btn .social-btn-icon { color: var(--blue-bright); background: rgba(77,184,255,0.1); border: 1px solid rgba(77,184,255,0.3); }
  .social-btn-icon {
    width: 68px; height: 68px; display: flex; align-items: center; justify-content: center; flex-shrink: 0;
    clip-path: polygon(0 0, calc(100% - 8px) 0, 100% 8px, 100% 100%, 8px 100%, 0 calc(100% - 8px));
    position: relative; z-index: 1; transition: transform 0.3s;
  }
  .social-big-btn:hover .social-btn-icon { transform: scale(1.1); }
  .social-btn-text { flex: 1; position: relative; z-index: 1; text-align: left; }
  .social-btn-platform { font-family: 'Bebas Neue', sans-serif; font-size: 2rem; letter-spacing: 4px; line-height: 1; margin-bottom: 4px; }
  .social-btn-handle { font-family: 'Rajdhani', sans-serif; font-size: 0.9rem; letter-spacing: 2px; color: var(--grey); }
  .social-btn-arrow { font-family: 'Bebas Neue', sans-serif; font-size: 2rem; color: rgba(255,255,255,0.15); position: relative; z-index: 1; transition: transform 0.3s, color 0.3s; }
  .social-big-btn:hover .social-btn-arrow { transform: translateX(6px); color: rgba(255,255,255,0.5); }

  .page-hero {
    min-height: 55vh; display: flex; align-items: flex-end;
    padding: 120px 5% 80px; position: relative; overflow: hidden;
  }
  .page-hero-bg { position: absolute; inset: 0; }
  .page-hero-content { position: relative; z-index: 2; }

  .highlights-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 2px; margin-top: 60px; }
  .highlight-item { background: var(--dark2); padding: 40px; position: relative; overflow: hidden; transition: all 0.3s; }
  .highlight-item:hover { background: var(--dark3); }
  .highlight-num { font-family: 'Bebas Neue', sans-serif; font-size: 5rem; color: rgba(77,184,255,0.08); position: absolute; top: -10px; right: 20px; line-height: 1; user-select: none; }
  .highlight-title { font-family: 'Rajdhani', sans-serif; font-size: 1.2rem; font-weight: 700; letter-spacing: 2px; text-transform: uppercase; margin-bottom: 12px; position: relative; z-index: 1; color: var(--white); }
  .highlight-desc { color: var(--grey); font-size: 0.9rem; line-height: 1.7; position: relative; z-index: 1; }

  footer { background: var(--dark); border-top: 1px solid rgba(77,184,255,0.15); padding: 60px 5% 40px; }
  .footer-top { display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; gap: 60px; margin-bottom: 60px; }
  .footer-brand p { color: var(--grey); font-size: 0.9rem; line-height: 1.8; margin-top: 16px; max-width: 280px; }
  .footer-col h4 { font-family: 'Rajdhani', sans-serif; font-weight: 700; letter-spacing: 3px; font-size: 0.85rem; text-transform: uppercase; color: var(--wave-cyan); margin-bottom: 20px; }
  .footer-col ul { list-style: none; }
  .footer-col ul li { margin-bottom: 10px; }
  .footer-col ul li a { color: var(--grey); text-decoration: none; font-size: 0.9rem; transition: color 0.3s; cursor: pointer; }
  .footer-col ul li a:hover { color: var(--white); }
  .footer-bottom { border-top: 1px solid rgba(255,255,255,0.06); padding-top: 30px; display: flex; justify-content: space-between; align-items: center; }
  .footer-bottom p { color: var(--grey); font-size: 0.8rem; letter-spacing: 1px; }

  /* ADMIN */
  #admin-overlay { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.88); z-index: 10000; backdrop-filter: blur(10px); align-items: center; justify-content: center; }
  #admin-overlay.open { display: flex; }
  #admin-login, #admin-panel { background: var(--dark2); border: 1px solid rgba(77,184,255,0.3); padding: 48px; width: 100%; position: relative; }
  #admin-login { max-width: 440px; }
  #admin-panel { max-width: 700px; max-height: 90vh; overflow-y: auto; }
  .admin-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 36px; }
  .admin-title { font-family: 'Bebas Neue', sans-serif; font-size: 2rem; letter-spacing: 4px; color: var(--blue-bright); }
  .admin-close { background: none; border: 1px solid rgba(255,255,255,0.2); color: var(--grey); width: 36px; height: 36px; cursor: pointer; font-size: 1.2rem; display: flex; align-items: center; justify-content: center; transition: all 0.3s; }
  .admin-close:hover { border-color: var(--blue-bright); color: var(--blue-bright); }
  .form-group { margin-bottom: 24px; }
  .form-group label { display: block; font-family: 'Rajdhani', sans-serif; font-size: 0.8rem; letter-spacing: 3px; text-transform: uppercase; color: var(--grey); margin-bottom: 8px; }
  .form-group input, .form-group textarea, .form-group select { width: 100%; background: var(--dark); border: 1px solid rgba(255,255,255,0.1); color: var(--white); padding: 12px 16px; font-family: 'Exo 2', sans-serif; font-size: 0.95rem; outline: none; transition: border-color 0.3s; resize: vertical; }
  .form-group input:focus, .form-group textarea:focus { border-color: var(--blue-bright); }
  .form-error { color: #e05555; font-size: 0.85rem; margin-top: 12px; display: none; }
  .admin-tabs { display: flex; gap: 4px; margin-bottom: 36px; border-bottom: 1px solid rgba(255,255,255,0.08); flex-wrap: wrap; }
  .admin-tab { background: none; border: none; color: var(--grey); font-family: 'Rajdhani', sans-serif; font-size: 0.85rem; letter-spacing: 2px; text-transform: uppercase; padding: 10px 20px; cursor: pointer; border-bottom: 2px solid transparent; margin-bottom: -1px; transition: all 0.3s; }
  .admin-tab.active { color: var(--blue-bright); border-bottom-color: var(--blue-bright); }
  .admin-tab:hover { color: var(--white); }
  .admin-section { display: none; }
  .admin-section.active { display: block; }
  .player-list { display: flex; flex-direction: column; gap: 12px; margin-bottom: 24px; }
  .player-item { display: flex; align-items: center; gap: 12px; background: var(--dark); padding: 12px 16px; border: 1px solid rgba(255,255,255,0.06); }
  .player-item span { flex: 1; font-family: 'Rajdhani', sans-serif; font-size: 0.95rem; }
  .role-tag { font-size: 0.75rem; letter-spacing: 1px; text-transform: uppercase; padding: 3px 8px; }
  .del-btn { background: none; border: 1px solid rgba(224,85,85,0.3); color: #e05555; cursor: pointer; padding: 4px 10px; font-size: 0.8rem; transition: all 0.3s; }
  .del-btn:hover { background: rgba(224,85,85,0.15); }
  .wl-btn-group { display: flex; gap: 10px; }
  .wl-btn { flex: 1; padding: 14px; font-family: 'Bebas Neue', sans-serif; font-size: 1.6rem; letter-spacing: 4px; border: 2px solid rgba(255,255,255,0.1); background: transparent; color: var(--grey); cursor: pointer; transition: all 0.2s; }
  .wl-btn.win { border-color: rgba(34,197,94,0.3); color: rgba(34,197,94,0.5); }
  .wl-btn.win.selected, .wl-btn.win:hover { background: rgba(34,197,94,0.15); border-color: #22c55e; color: #22c55e; }
  .wl-btn.loss { border-color: rgba(224,85,85,0.3); color: rgba(224,85,85,0.5); }
  .wl-btn.loss.selected, .wl-btn.loss:hover { background: rgba(224,85,85,0.15); border-color: #e05555; color: #e05555; }

  .save-indicator { position: fixed; bottom: 30px; right: 30px; background: var(--blue); color: white; padding: 12px 24px; font-family: 'Rajdhani', sans-serif; font-size: 0.9rem; letter-spacing: 2px; text-transform: uppercase; z-index: 99999; transform: translateY(80px); transition: transform 0.4s; pointer-events: none; }
  .save-indicator.show { transform: translateY(0); }

  @keyframes fadeUp { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
  .reveal { opacity: 0; transform: translateY(40px); transition: opacity 0.7s, transform 0.7s; }
  .reveal.visible { opacity: 1; transform: translateY(0); }

  ::-webkit-scrollbar { width: 6px; }
  ::-webkit-scrollbar-track { background: var(--dark); }
  ::-webkit-scrollbar-thumb { background: var(--blue); }

  @media (max-width: 768px) {
    .nav-links { display: none; }
    .team-header { grid-template-columns: 1fr; }
    .footer-top { grid-template-columns: 1fr 1fr; }
    .hero-title { font-size: 3rem; }
    #admin-panel { padding: 24px; }
    section { padding: 70px 5%; }
    .winloss-board { grid-template-columns: 1fr; }
  }
</style>
</head>
<body>

<div id="cursor"></div>
<div id="cursor-ring"></div>
<div class="save-indicator" id="save-indicator">✓ GUARDADO</div>

<nav>
  <a class="logo" href="#" onclick="showPage('home')">
    <img class="logo-shield" id="nav-logo-img" src="gsl-logo.png" alt="GSL">
    GSL <span>RL TEAM</span>
  </a>
  <ul class="nav-links">
    <li><a href="#" onclick="showPage('home')" id="nav-home" class="active">Home</a></li>
    <li><a href="#" onclick="showPage('rl')" id="nav-rl">⚡ Rocket League</a></li>
    <li><a href="#" onclick="showPage('pt')" id="nav-pt">🇵🇹 Portugal RL</a></li>
    <li><a href="#" onclick="showPage('info')" id="nav-info">ℹ️ Info</a></li>
  </ul>
  <button class="nav-admin-btn" onclick="openAdmin()">⚙ ADMIN</button>
</nav>

<!-- HOME -->
<div class="page active" id="page-home">
  <section class="hero">
    <div class="hero-bg"></div>
    <div class="hero-grid"></div>
    <div class="hero-glow-pulse"></div>
    <div class="hero-orbs">
      <div class="orb"></div>
      <div class="orb"></div>
      <div class="orb"></div>
    </div>
    <div class="hero-content">
      <div class="hero-shield-wrap">
        <img class="hero-shield" id="hero-logo-img" src="gsl-logo.png" alt="GSL">
      </div>
      <span class="hero-tag">Esports Organization Est. 10/07/2026</span>
      <h1 class="hero-title">GSL<br><span class="blue">RL TEAM</span></h1>
      <p class="hero-subtitle" id="home-subtitle">Força · Foco · Vitória</p>
      <div class="hero-divider"></div>
      <p class="hero-desc" id="home-desc">Uma organização de esports em ascensão, dedicada à excelência competitiva no Rocket League. Presentes na Europa, construindo campeões com intensidade e determinação.</p>
      <div class="hero-btns">
        <button class="btn btn-primary" onclick="showPage('rl')">⚡ Rocket League</button>
        <button class="btn btn-outline" onclick="showPage('pt')">🇵🇹 Portugal RL</button>
      </div>
      <div class="hero-social">
        <div class="hero-social-label">SEGUE-NOS</div>
        <div class="hero-social-btns">
          <a href="https://discord.gg/ZsPRgNJkqc" target="_blank" rel="noopener" class="hero-social-btn discord-hero-btn">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor"><path d="M20.317 4.37a19.79 19.79 0 0 0-4.885-1.515.074.074 0 0 0-.079.037c-.21.375-.444.864-.608 1.25a18.27 18.27 0 0 0-5.487 0 12.64 12.64 0 0 0-.617-1.25.077.077 0 0 0-.079-.037A19.736 19.736 0 0 0 3.677 4.37a.07.07 0 0 0-.032.027C.533 9.046-.32 13.58.099 18.057a.082.082 0 0 0 .031.057 19.9 19.9 0 0 0 5.993 3.03.078.078 0 0 0 .084-.028 14.09 14.09 0 0 0 1.226-1.994.076.076 0 0 0-.041-.106 13.107 13.107 0 0 1-1.872-.892.077.077 0 0 1-.008-.128c.126-.094.252-.192.372-.291a.074.074 0 0 1 .077-.01c3.928 1.793 8.18 1.793 12.061 0a.073.073 0 0 1 .078.01c.12.099.246.198.373.292a.077.077 0 0 1-.006.127 12.3 12.3 0 0 1-1.873.892.076.076 0 0 0-.04.107c.36.698.772 1.362 1.225 1.993a.076.076 0 0 0 .084.028 19.84 19.84 0 0 0 6.002-3.03.077.077 0 0 0 .032-.055c.5-5.177-.838-9.674-3.549-13.66a.061.061 0 0 0-.031-.028zM8.02 15.33c-1.183 0-2.157-1.085-2.157-2.419 0-1.333.955-2.418 2.157-2.418 1.21 0 2.176 1.094 2.157 2.418 0 1.334-.955 2.419-2.157 2.419zm7.975 0c-1.183 0-2.157-1.085-2.157-2.419 0-1.333.955-2.418 2.157-2.418 1.21 0 2.176 1.094 2.157 2.418 0 1.334-.946 2.419-2.157 2.419z"/></svg>
            <span>Discord</span>
            <span class="hero-social-handle">Entrar</span>
          </a>
          <a href="https://www.tiktok.com/@team_gsl_" target="_blank" rel="noopener" class="hero-social-btn tiktok-hero-btn">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor"><path d="M19.59 6.69a4.83 4.83 0 0 1-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 0 1-2.88 2.5 2.89 2.89 0 0 1-2.89-2.89 2.89 2.89 0 0 1 2.89-2.89c.28 0 .54.04.79.1V9.01a6.33 6.33 0 0 0-.79-.05 6.34 6.34 0 0 0-6.34 6.34 6.34 6.34 0 0 0 6.34 6.34 6.34 6.34 0 0 0 6.33-6.34V8.69a8.18 8.18 0 0 0 4.78 1.52V6.76a4.85 4.85 0 0 1-1.01-.07z"/></svg>
            <span>TikTok</span>
            <span class="hero-social-handle">@team_gsl_</span>
          </a>
        </div>
      </div>
    </div>
  </section>

  <div class="stats-bar reveal">
    <div class="stat-item"><div class="stat-number" id="stat-members">EU</div><div class="stat-label">Equipas Ativas</div></div>
    <div class="stat-item"><div class="stat-number" id="stat-players">45</div><div class="stat-label">Jogadores Ativos</div></div>
    <div class="stat-item"><div class="stat-number" id="stat-tournaments">7</div><div class="stat-label">Xteams</div></div>
    <div class="stat-item"><div class="stat-number" id="stat-wins">10/07/2026</div><div class="stat-label">Fundação</div></div>
  </div>

  <section>
    <div class="reveal">
      <span class="section-tag">Sobre Nós</span>
      <h2 class="section-title" id="about-title">A NOSSA MISSÃO</h2>
      <p class="section-desc" id="about-text">A GSL é mais do que uma organização de esports. Somos uma equipa unida pela paixão ao Rocket League e pela ambição de dominar a Europa, jogando sempre ao mais alto nível.</p>
    </div>
    <div class="cards-grid">
      <div class="card reveal"><div class="card-icon">⚡</div><h3 id="val1-title">Growth</h3><p id="val1-text">De Gold a SSL — desenvolvemos cada jogador com treino estruturado e análise profunda do jogo.</p></div>
      <div class="card reveal"><div class="card-icon">📈</div><h3 id="val2-title">Strength</h3><p id="val2-text">Jogamos com intensidade máxima: imparáveis, consistentes e poderosos. A pressão constante é o que nos define.</p></div>
      <div class="card reveal"><div class="card-icon">🏆</div><h3 id="val3-title">Legacy</h3><p id="val3-text">Mentalidade de campeão em cada partida. Construímos um legado que vai além dos resultados.</p></div>
    </div>
  </section>

  <section class="reveal" style="padding: 80px 5%; text-align:center; position:relative; overflow:hidden;">
    <div style="position:absolute;inset:0;background:radial-gradient(ellipse 70% 60% at 50% 50%, rgba(77,184,255,0.07) 0%, transparent 70%);pointer-events:none;"></div>
    <span class="section-tag" style="display:block;text-align:center;">Junta-te a Nós</span>
    <h2 class="section-title" style="text-align:center;margin-bottom:16px;">COMUNIDADE GSL</h2>
    <p style="color:var(--grey);max-width:500px;margin:0 auto 56px;font-size:1rem;line-height:1.7;">Segue a GSL e faz parte da família. Scrims, torneios e uma comunidade que joga para ganhar.</p>
    <div class="social-big-btns">
      <a href="https://discord.gg/ZsPRgNJkqc" target="_blank" rel="noopener" class="social-big-btn discord-btn">
        <div class="social-btn-glow discord-glow"></div>
        <div class="social-btn-icon">
          <svg width="36" height="36" viewBox="0 0 24 24" fill="currentColor"><path d="M20.317 4.37a19.79 19.79 0 0 0-4.885-1.515.074.074 0 0 0-.079.037c-.21.375-.444.864-.608 1.25a18.27 18.27 0 0 0-5.487 0 12.64 12.64 0 0 0-.617-1.25.077.077 0 0 0-.079-.037A19.736 19.736 0 0 0 3.677 4.37a.07.07 0 0 0-.032.027C.533 9.046-.32 13.58.099 18.057a.082.082 0 0 0 .031.057 19.9 19.9 0 0 0 5.993 3.03.078.078 0 0 0 .084-.028 14.09 14.09 0 0 0 1.226-1.994.076.076 0 0 0-.041-.106 13.107 13.107 0 0 1-1.872-.892.077.077 0 0 1-.008-.128c.126-.094.252-.192.372-.291a.074.074 0 0 1 .077-.01c3.928 1.793 8.18 1.793 12.061 0a.073.073 0 0 1 .078.01c.12.099.246.198.373.292a.077.077 0 0 1-.006.127 12.3 12.3 0 0 1-1.873.892.076.076 0 0 0-.04.107c.36.698.772 1.362 1.225 1.993a.076.076 0 0 0 .084.028 19.84 19.84 0 0 0 6.002-3.03.077.077 0 0 0 .032-.055c.5-5.177-.838-9.674-3.549-13.66a.061.061 0 0 0-.031-.028zM8.02 15.33c-1.183 0-2.157-1.085-2.157-2.419 0-1.333.955-2.418 2.157-2.418 1.21 0 2.176 1.094 2.157 2.418 0 1.334-.955 2.419-2.157 2.419zm7.975 0c-1.183 0-2.157-1.085-2.157-2.419 0-1.333.955-2.418 2.157-2.418 1.21 0 2.176 1.094 2.157 2.418 0 1.334-.946 2.419-2.157 2.419z"/></svg>
        </div>
        <div class="social-btn-text">
          <div class="social-btn-platform">Discord</div>
          <div class="social-btn-handle">Junta-te ao servidor</div>
        </div>
        <div class="social-btn-arrow">→</div>
      </a>
      <a href="https://www.tiktok.com/@team_gsl_" target="_blank" rel="noopener" class="social-big-btn tiktok-btn">
        <div class="social-btn-glow tiktok-glow"></div>
        <div class="social-btn-icon">
          <svg width="36" height="36" viewBox="0 0 24 24" fill="currentColor"><path d="M19.59 6.69a4.83 4.83 0 0 1-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 0 1-2.88 2.5 2.89 2.89 0 0 1-2.89-2.89 2.89 2.89 0 0 1 2.89-2.89c.28 0 .54.04.79.1V9.01a6.33 6.33 0 0 0-.79-.05 6.34 6.34 0 0 0-6.34 6.34 6.34 6.34 0 0 0 6.34 6.34 6.34 6.34 0 0 0 6.33-6.34V8.69a8.18 8.18 0 0 0 4.78 1.52V6.76a4.85 4.85 0 0 1-1.01-.07z"/></svg>
        </div>
        <div class="social-btn-text">
          <div class="social-btn-platform">TikTok</div>
          <div class="social-btn-handle">@team_gsl_</div>
        </div>
        <div class="social-btn-arrow">→</div>
      </a>
    </div>
  </section>

  <section style="padding-top:0;">
    <div class="reveal"><span class="section-tag">Os Nossos Jogos</span><h2 class="section-title">COMPETIÇÕES ATIVAS</h2></div>
    <div class="highlights-grid reveal">
      <div class="highlight-item" style="cursor:pointer" onclick="showPage('rl')">
        <div class="highlight-num">01</div>
        <div class="highlight-title">Rocket League</div>
        <p class="highlight-desc">Equipa principal competitiva em formato 2v2/3v3. Gold a SSL. Treino estruturado e mentalidade de campeão.</p>
        <br><span style="color:var(--wave-cyan);font-size:0.8rem;letter-spacing:2px;font-family:'Rajdhani',sans-serif;">VER EQUIPA →</span>
      </div>
      <div class="highlight-item" style="cursor:pointer" onclick="showPage('pt')">
        <div class="highlight-num">02</div>
        <div class="highlight-title">Portugal RL</div>
        <p class="highlight-desc">Representação portuguesa. Scrims, torneios nacionais e crescimento dentro da comunidade ibérica.</p>
        <br><span style="color:var(--wave-cyan);font-size:0.8rem;letter-spacing:2px;font-family:'Rajdhani',sans-serif;">VER EQUIPA →</span>
      </div>
      <div class="highlight-item">
        <div class="highlight-num">03</div>
        <div class="highlight-title">Recrutamento Aberto</div>
        <p class="highlight-desc">Aceitamos jogadores de Gold a SSL. Segue-nos no TikTok e candidata-te à GSL.</p>
        <br><a href="https://www.tiktok.com/@team_gsl_" target="_blank" style="color:var(--wave-cyan);font-size:0.8rem;letter-spacing:2px;font-family:'Rajdhani',sans-serif;text-decoration:none;">SEGUIR NO TIKTOK →</a>
      </div>
    </div>
  </section>

  <footer>
    <div class="footer-top">
      <div class="footer-brand">
        <div class="logo" style="font-size:1.3rem;">GSL <span>RL TEAM</span></div>
        <p>Organização de esports dedicada à excelência, crescimento e comunidade no Rocket League.</p>
      </div>
      <div class="footer-col"><h4>Páginas</h4><ul><li><a onclick="showPage('home')">Home</a></li><li><a onclick="showPage('rl')">Rocket League</a></li><li><a onclick="showPage('pt')">Portugal RL</a></li><li><a onclick="showPage('info')">Informações</a></li></ul></div>
      <div class="footer-col"><h4>Equipas</h4><ul><li><a>Roster Principal</a></li><li><a>2v2 Division</a></li><li><a>Coaching Staff</a></li></ul></div>
      <div class="footer-col"><h4>Contacto</h4><ul>
        <li><a href="https://discord.gg/ZsPRgNJkqc" target="_blank" rel="noopener" style="display:flex;align-items:center;gap:6px;">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M20.317 4.37a19.79 19.79 0 0 0-4.885-1.515.074.074 0 0 0-.079.037c-.21.375-.444.864-.608 1.25a18.27 18.27 0 0 0-5.487 0 12.64 12.64 0 0 0-.617-1.25.077.077 0 0 0-.079-.037A19.736 19.736 0 0 0 3.677 4.37a.07.07 0 0 0-.032.027C.533 9.046-.32 13.58.099 18.057a.082.082 0 0 0 .031.057 19.9 19.9 0 0 0 5.993 3.03.078.078 0 0 0 .084-.028 14.09 14.09 0 0 0 1.226-1.994.076.076 0 0 0-.041-.106 13.107 13.107 0 0 1-1.872-.892.077.077 0 0 1-.008-.128c.126-.094.252-.192.372-.291a.074.074 0 0 1 .077-.01c3.928 1.793 8.18 1.793 12.061 0a.073.073 0 0 1 .078.01c.12.099.246.198.373.292a.077.077 0 0 1-.006.127 12.3 12.3 0 0 1-1.873.892.076.076 0 0 0-.04.107c.36.698.772 1.362 1.225 1.993a.076.076 0 0 0 .084.028 19.84 19.84 0 0 0 6.002-3.03.077.077 0 0 0 .032-.055c.5-5.177-.838-9.674-3.549-13.66a.061.061 0 0 0-.031-.028zM8.02 15.33c-1.183 0-2.157-1.085-2.157-2.419 0-1.333.955-2.418 2.157-2.418 1.21 0 2.176 1.094 2.157 2.418 0 1.334-.955 2.419-2.157 2.419zm7.975 0c-1.183 0-2.157-1.085-2.157-2.419 0-1.333.955-2.418 2.157-2.418 1.21 0 2.176 1.094 2.157 2.418 0 1.334-.946 2.419-2.157 2.419z"/></svg>
          Discord
        </a></li>
        <li><a href="https://www.tiktok.com/@team_gsl_" target="_blank" rel="noopener" style="display:flex;align-items:center;gap:6px;">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M19.59 6.69a4.83 4.83 0 0 1-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 0 1-2.88 2.5 2.89 2.89 0 0 1-2.89-2.89 2.89 2.89 0 0 1 2.89-2.89c.28 0 .54.04.79.1V9.01a6.33 6.33 0 0 0-.79-.05 6.34 6.34 0 0 0-6.34 6.34 6.34 6.34 0 0 0 6.34 6.34 6.34 6.34 0 0 0 6.33-6.34V8.69a8.18 8.18 0 0 0 4.78 1.52V6.76a4.85 4.85 0 0 1-1.01-.07z"/></svg>
          TikTok
        </a></li>
        <li><a href="https://ticketgsl.freshdesk.com/a/tickets/new" target="_blank" rel="noopener">Abrir Ticket</a></li>
      </ul></div>
    </div>
    <div class="footer-bottom"><p>© 2026 GSL. Todos os direitos reservados.</p><p style="color:var(--blue-bright);">GSL | RL TEAM</p></div>
  </footer>
</div>

<!-- ROCKET LEAGUE PAGE -->
<div class="page" id="page-rl">
  <div class="page-hero">
    <div class="page-hero-bg" style="background: radial-gradient(ellipse 100% 80% at 30% 50%, rgba(77,184,255,0.18) 0%, transparent 70%), var(--dark);"></div>
    <div class="hero-grid" style="mask-image: radial-gradient(ellipse 60% 80% at 20% 50%, black, transparent);"></div>
    <div class="page-hero-content">
      <span class="section-tag">GSL</span>
      <h1 class="hero-title" style="font-size:clamp(3rem,10vw,8rem);opacity:1;animation:none;" id="rl-title">ROCKET<br><span class="blue">LEAGUE</span></h1>
      <p style="color:var(--grey);letter-spacing:2px;font-family:'Rajdhani',sans-serif;font-size:1rem;text-transform:uppercase;margin-top:16px;" id="rl-subtitle">Equipa Principal · 2v2/3v3 · Gold — SSL</p>
    </div>
  </div>
  <div class="stats-bar" style="grid-template-columns:repeat(4,1fr);">
    <div class="stat-item"><div class="stat-number" id="rl-stat1">Gold-SSL</div><div class="stat-label">Recrutamos</div></div>
    <div class="stat-item"><div class="stat-number" id="rl-stat2">45</div><div class="stat-label">Jogadores Ativos</div></div>
    <div class="stat-item"><div class="stat-number" id="rl-stat3">2v2/3v3</div><div class="stat-label">Formato Principal</div></div>
    <div class="stat-item"><div class="stat-number" id="rl-stat4">100%</div><div class="stat-label">Dedicação</div></div>
  </div>
  <section>
    <div class="team-header">
      <div>
        <span class="section-tag">Sobre a Equipa</span>
        <h2 class="section-title" id="rl-about-title">SÉRIO. ORGANIZADO. IMPARÁVEL.</h2>
        <p class="section-desc" id="rl-about-text">A equipa de Rocket League da GSL opera com a mentalidade de um clube profissional. Treinos estruturados, análise de replays, comunicação afinada. Recrutamos de Gold a SSL.</p>
        <div style="margin-top:32px;"><button class="btn btn-primary" onclick="showPage('home')">← Voltar ao Início</button></div>
      </div>
      <div class="team-visual">
        <div class="team-visual-text">GSL</div>
        <img src="gsl-logo.png" style="width:130px;height:130px;border-radius:12px;object-fit:cover;border:2px solid rgba(77,184,255,0.4);box-shadow:0 0 40px var(--blue-glow);position:relative;z-index:1;" alt="GSL">
      </div>
    </div>
  </section>
  <section style="padding-top:0;">
    <span class="section-tag">Filosofia</span>
    <h2 class="section-title" id="rl-philosophy-title">COMO TRABALHAMOS</h2>
    <div class="cards-grid">
      <div class="card"><div class="card-icon">🎮</div><h3 id="rl-p1-title">Treino Diário</h3><p id="rl-p1-text">Sessões estruturadas focadas em mecânicas 2v2, rotações e comunicação. A consistência cria campeões.</p></div>
      <div class="card"><div class="card-icon">📊</div><h3 id="rl-p2-title">Análise de Jogo</h3><p id="rl-p2-text">Review de replays, identificação de erros e estudo de adversários. Evoluir com dados reais.</p></div>
      <div class="card"><div class="card-icon">⚡</div><h3 id="rl-p3-title">Pressão GSL</h3><p id="rl-p3-text">Jogamos sob pressão constante, intensidade máxima, sem recuar. É a nossa identidade.</p></div>
    </div>
  </section>
  <footer><div class="footer-bottom" style="border:none;padding-top:0;"><p style="color:var(--grey);">© 2026 GSL.</p><p style="color:var(--blue-bright);">ROCKET LEAGUE DIVISION</p></div></footer>
</div>

<!-- PORTUGAL RL PAGE -->
<div class="page" id="page-pt">
  <div class="page-hero">
    <div class="page-hero-bg" style="background: radial-gradient(ellipse 100% 80% at 70% 50%, rgba(77,184,255,0.22) 0%, transparent 70%), var(--dark);"></div>
    <div class="hero-grid" style="mask-image: radial-gradient(ellipse 60% 80% at 80% 50%, black, transparent);"></div>
    <div class="page-hero-content">
      <span class="section-tag">🇵🇹 Portugal Division</span>
      <h1 class="hero-title" style="font-size:clamp(3rem,10vw,8rem);opacity:1;animation:none;" id="pt-title">PORTUGAL<br><span class="blue">RL</span></h1>
      <p style="color:var(--grey);letter-spacing:2px;font-family:'Rajdhani',sans-serif;font-size:1rem;text-transform:uppercase;margin-top:16px;" id="pt-subtitle">Scrims · Torneios · Comunidade Nacional</p>
    </div>
  </div>
  <div class="stats-bar">
    <div class="stat-item"><div class="stat-number" id="pt-stat1">Portugal</div><div class="stat-label">Região</div></div>
    <div class="stat-item"><div class="stat-number" id="pt-stat2">EU</div><div class="stat-label">Divisão RLCS</div></div>
    <div class="stat-item"><div class="stat-number" id="pt-stat3">6</div><div class="stat-label">WINS</div></div>
    <div class="stat-item"><div class="stat-number" id="pt-stat4">1</div><div class="stat-label">LOSSES</div></div>
  </div>
  <section>
    <div class="team-header">
      <div class="team-visual" style="order:-1;background:linear-gradient(135deg,var(--dark2),var(--navy));">
        <div class="team-visual-text" style="font-size:6rem;">PT</div>
        <div class="team-badge" style="font-size:2rem;">🇵🇹</div>
      </div>
      <div>
        <span class="section-tag">Portugal RL</span>
        <h2 class="section-title" id="pt-about-title">AGRESSIVO. DINÂMICO. IMPARÁVEL.</h2>
        <p class="section-desc" id="pt-about-text">Representamos Portugal com orgulho. A GSL joga com intensidade, criatividade e determinação — as marcas dos melhores da cena nacional.</p>
        <div style="margin-top:32px;"><button class="btn btn-primary" onclick="showPage('home')">← Voltar ao Início</button></div>
      </div>
    </div>
    <span class="section-tag">Resultados</span>
    <h3 class="section-title" style="font-size:2.5rem;">REGISTO DE PARTIDAS</h3>
    <div class="matches-section" id="pt-matches-section"></div>
  </section>
  <footer><div class="footer-bottom" style="border:none;padding-top:0;"><p style="color:var(--grey);">© 2026 GSL.</p><p style="color:var(--blue-bright);">PORTUGAL RL DIVISION</p></div></footer>
</div>

<!-- INFO PAGE -->
<div class="page" id="page-info">
  <div class="page-hero">
    <div class="page-hero-bg" style="background: radial-gradient(ellipse 100% 80% at 50% 50%, rgba(77,184,255,0.18) 0%, transparent 70%), var(--dark);"></div>
    <div class="hero-grid" style="mask-image: radial-gradient(ellipse 60% 80% at 50% 50%, black, transparent);"></div>
    <div class="page-hero-content">
      <span class="section-tag">GSL</span>
      <h1 class="hero-title" style="font-size:clamp(3rem,10vw,8rem);opacity:1;animation:none;">INFORMAÇÕES<br><span class="blue">DO SITE</span></h1>
      <p style="color:var(--grey);letter-spacing:2px;font-family:'Rajdhani',sans-serif;font-size:1rem;text-transform:uppercase;margin-top:16px;">Estado, Versão e Suporte</p>
    </div>
  </div>
  <section>
    <div class="card" style="max-width:700px;cursor:default;">
      <div class="card-icon">ℹ️</div>
      <h3>Estado do Site</h3>
      <p style="line-height:2;">
        📅 Última atualização: 18/07/2026<br>
        🚀 Versão: 1.0.0<br>
        🔄 O site é atualizado regularmente (cada mês) com melhorias e novas funcionalidades.<br>
        ⚠️ O site encontra-se em desenvolvimento, pelo que algumas funcionalidades poderão mudar.<br>
        ✨ Alterações recentes:
      </p>
      <ul style="color:var(--grey);line-height:1.9;margin-left:20px;">
        <li>Melhorias de desempenho.</li>
        <li>Correção de erros.</li>
        <li>Nova interface.</li>
      </ul>
      <div style="margin-top:32px;"><button class="btn btn-primary" onclick="window.open('https://ticketgsl.freshdesk.com/a/tickets/new','_blank')">🎫 Abrir Ticket de Suporte</button></div>
    </div>
    <div style="margin-top:40px;"><button class="btn btn-outline" onclick="showPage('home')">← Voltar ao Início</button></div>
  </section>
  <footer><div class="footer-bottom" style="border:none;padding-top:0;"><p style="color:var(--grey);">© 2026 GSL.</p><p style="color:var(--blue-bright);">SITE INFO</p></div></footer>
</div>

<!-- ADMIN -->
<div id="admin-overlay">
  <div id="admin-login">
    <div class="admin-header">
      <div class="admin-title">ADMIN</div>
      <button class="admin-close" onclick="closeAdmin()">✕</button>
    </div>
    <div class="form-group"><label>Utilizador</label><input type="text" id="login-user" placeholder="admin" /></div>
    <div class="form-group"><label>Password</label><input type="password" id="login-pass" placeholder="••••••••" onkeydown="if(event.key==='Enter')doLogin()" /></div>
    <p class="form-error" id="login-error">Credenciais inválidas.</p>
    <button class="btn btn-primary" style="width:100%" onclick="doLogin()">ENTRAR</button>
  </div>
  <div id="admin-panel" style="display:none;">
    <div class="admin-header">
      <div class="admin-title">PAINEL DE ADMIN</div>
      <button class="admin-close" onclick="closeAdmin()">✕</button>
    </div>
    <div class="admin-tabs">
      <button class="admin-tab active" onclick="switchTab('home-tab',this)">Home</button>
      <button class="admin-tab" onclick="switchTab('rl-tab',this)">⚡ Rocket League</button>
      <button class="admin-tab" onclick="switchTab('pt-tab',this)">🇵🇹 Portugal RL</button>
      <button class="admin-tab" onclick="switchTab('stats-tab',this)">Estatísticas</button>
    </div>

    <div class="admin-section active" id="home-tab">
      <h3 style="font-family:'Rajdhani',sans-serif;letter-spacing:3px;font-size:1rem;text-transform:uppercase;color:var(--grey);margin-bottom:24px;">HOME — CONTEÚDO</h3>
      <div class="form-group"><label>Subtítulo Hero</label><input type="text" id="a-home-subtitle" /></div>
      <div class="form-group"><label>Descrição Hero</label><textarea id="a-home-desc" rows="3"></textarea></div>
      <div class="form-group"><label>Título "A Nossa Missão"</label><input type="text" id="a-about-title" /></div>
      <div class="form-group"><label>Texto "Sobre Nós"</label><textarea id="a-about-text" rows="4"></textarea></div>
      <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:16px;">
        <div><div class="form-group"><label>Valor 1 — Título</label><input type="text" id="a-val1-title" /></div><div class="form-group"><label>Valor 1 — Texto</label><textarea id="a-val1-text" rows="3"></textarea></div></div>
        <div><div class="form-group"><label>Valor 2 — Título</label><input type="text" id="a-val2-title" /></div><div class="form-group"><label>Valor 2 — Texto</label><textarea id="a-val2-text" rows="3"></textarea></div></div>
        <div><div class="form-group"><label>Valor 3 — Título</label><input type="text" id="a-val3-title" /></div><div class="form-group"><label>Valor 3 — Texto</label><textarea id="a-val3-text" rows="3"></textarea></div></div>
      </div>
      <button class="btn btn-primary" onclick="saveHomeContent()">GUARDAR ALTERAÇÕES</button>
    </div>

    <div class="admin-section" id="rl-tab">
      <h3 style="font-family:'Rajdhani',sans-serif;letter-spacing:3px;font-size:1rem;text-transform:uppercase;color:var(--grey);margin-bottom:24px;">⚡ ROCKET LEAGUE — CONTEÚDO</h3>
      <div class="form-group"><label>Subtítulo</label><input type="text" id="a-rl-subtitle" /></div>
      <div class="form-group"><label>Título "Sobre"</label><input type="text" id="a-rl-about-title" /></div>
      <div class="form-group"><label>Texto "Sobre"</label><textarea id="a-rl-about-text" rows="3"></textarea></div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;">
        <div class="form-group"><label>Stat 1 (Recrutamos)</label><input type="text" id="a-rl-stat1" /></div>
        <div class="form-group"><label>Stat 2 (Jogadores)</label><input type="text" id="a-rl-stat2" /></div>
        <div class="form-group"><label>Stat 3 (Formato)</label><input type="text" id="a-rl-stat3" /></div>
        <div class="form-group"><label>Stat 4 (Dedicação)</label><input type="text" id="a-rl-stat4" /></div>
      </div>
      <button class="btn btn-primary" onclick="saveRLContent()">GUARDAR</button>
    </div>

    <div class="admin-section" id="pt-tab">
      <h3 style="font-family:'Rajdhani',sans-serif;letter-spacing:3px;font-size:1rem;text-transform:uppercase;color:var(--grey);margin-bottom:24px;">🇵🇹 PORTUGAL RL — CONTEÚDO</h3>
      <div class="form-group"><label>Subtítulo</label><input type="text" id="a-pt-subtitle" /></div>
      <div class="form-group"><label>Título "Sobre"</label><input type="text" id="a-pt-about-title" /></div>
      <div class="form-group"><label>Texto "Sobre"</label><textarea id="a-pt-about-text" rows="3"></textarea></div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;">
        <div class="form-group"><label>Stat 1 (Região)</label><input type="text" id="a-pt-stat1" /></div>
        <div class="form-group"><label>Stat 2 (Divisão)</label><input type="text" id="a-pt-stat2" /></div>
        <div class="form-group"><label>Stat 3 (WINS)</label><input type="text" id="a-pt-stat3" /></div>
        <div class="form-group"><label>Stat 4 (LOSSES)</label><input type="text" id="a-pt-stat4" /></div>
      </div>
      <button class="btn btn-primary" onclick="savePTContent()">GUARDAR</button>
      <hr style="border:none;border-top:1px solid rgba(255,255,255,0.06);margin:32px 0;">
      <h3 style="font-family:'Rajdhani',sans-serif;letter-spacing:3px;font-size:1rem;text-transform:uppercase;color:var(--grey);margin-bottom:24px;">PARTIDAS — PORTUGAL RL</h3>
      <div class="player-list" id="a-pt-matches"></div>
      <div class="form-group" style="margin-bottom:8px;"><label>Adicionar resultado</label></div>
      <div class="wl-btn-group" id="pt-wl-group">
        <button class="wl-btn win" onclick="selectWL('pt','w',this)">W</button>
        <button class="wl-btn loss" onclick="selectWL('pt','l',this)">L</button>
      </div>
      <input type="hidden" id="pt-add-wl" value="" />
      <br><button class="btn btn-primary" style="margin-top:16px;" onclick="addMatch('pt')">+ ADICIONAR</button>
    </div>

    <div class="admin-section" id="stats-tab">
      <h3 style="font-family:'Rajdhani',sans-serif;letter-spacing:3px;font-size:1rem;text-transform:uppercase;color:var(--grey);margin-bottom:24px;">ESTATÍSTICAS GLOBAIS</h3>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;">
        <div class="form-group"><label>Regiões Ativas</label><input type="text" id="a-stat-members" /></div>
        <div class="form-group"><label>Jogadores</label><input type="text" id="a-stat-players" /></div>
        <div class="form-group"><label>Xteams</label><input type="text" id="a-stat-tournaments" /></div>
        <div class="form-group"><label>Fundação</label><input type="text" id="a-stat-wins" /></div>
      </div>
      <button class="btn btn-primary" onclick="saveStats()">GUARDAR ESTATÍSTICAS</button>
    </div>
  </div>
</div>

<script>
const ALL_FIELDS = [
  ['a-home-subtitle','home-subtitle'],['a-home-desc','home-desc'],
  ['a-about-title','about-title'],['a-about-text','about-text'],
  ['a-val1-title','val1-title'],['a-val1-text','val1-text'],
  ['a-val2-title','val2-title'],['a-val2-text','val2-text'],
  ['a-val3-title','val3-title'],['a-val3-text','val3-text'],
  ['a-rl-subtitle','rl-subtitle'],
  ['a-rl-about-title','rl-about-title'],['a-rl-about-text','rl-about-text'],
  ['a-rl-stat1','rl-stat1'],['a-rl-stat2','rl-stat2'],['a-rl-stat3','rl-stat3'],['a-rl-stat4','rl-stat4'],
  ['a-pt-subtitle','pt-subtitle'],
  ['a-pt-about-title','pt-about-title'],['a-pt-about-text','pt-about-text'],
  ['a-pt-stat1','pt-stat1'],['a-pt-stat2','pt-stat2'],['a-pt-stat3','pt-stat3'],['a-pt-stat4','pt-stat4'],
  ['a-stat-members','stat-members'],['a-stat-players','stat-players'],
  ['a-stat-tournaments','stat-tournaments'],['a-stat-wins','stat-wins'],
];

function buildMatches(wins, losses) {
  const arr = [];
  for (let i = 0; i < wins; i++) arr.push({ wl: 'w' });
  for (let i = 0; i < losses; i++) arr.push({ wl: 'l' });
  return arr;
}

const state = {
  matches: { pt: buildMatches(6, 1) }
};

async function sharedGet(key) {
  try { const r = await window.storage.get(key, true); return r ? r.value : null; } catch(e) { return null; }
}
async function sharedSet(key, value) {
  try { await window.storage.set(key, value, true); } catch(e) {}
}

async function loadFromStorage() {
  for (const [, pageId] of ALL_FIELDS) {
    const saved = await sharedGet('arl_' + pageId);
    const el = document.getElementById(pageId);
    if (saved && el) el.textContent = saved;
  }
  const saved = await sharedGet('arl_matches_pt');
  if (saved) state.matches.pt = JSON.parse(saved);
  renderMatches();
}

// Cursor
const cursor = document.getElementById('cursor');
const ring = document.getElementById('cursor-ring');
document.addEventListener('mousemove', e => {
  cursor.style.left = e.clientX - 6 + 'px';
  cursor.style.top = e.clientY - 6 + 'px';
  ring.style.left = e.clientX - 18 + 'px';
  ring.style.top = e.clientY - 18 + 'px';
});
document.addEventListener('mousedown', () => cursor.style.transform = 'scale(1.8)');
document.addEventListener('mouseup', () => cursor.style.transform = 'scale(1)');

function showPage(id) {
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
  document.querySelectorAll('.nav-links a').forEach(a => a.classList.remove('active'));
  document.getElementById('page-' + id).classList.add('active');
  document.getElementById('nav-' + id)?.classList.add('active');
  window.scrollTo({ top: 0, behavior: 'smooth' });
  renderMatches();
}

const observer = new IntersectionObserver(entries => {
  entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible'); });
}, { threshold: 0.1 });
document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

function renderMatches() {
  const container = document.getElementById('pt-matches-section');
  if (!container) return;
  const matches = state.matches.pt;
  const wins = matches.filter(m => m.wl === 'w').length;
  const losses = matches.filter(m => m.wl === 'l').length;
  const total = wins + losses;
  const ratio = total > 0 ? Math.round((wins / total) * 100) : 0;
  const lossRatio = total > 0 ? Math.round((losses / total) * 100) : 0;
  container.innerHTML = `
    <div class="winloss-board">
      <div class="wl-box wins"><div class="wl-number">${wins}</div><div class="wl-label">Vitórias</div><div class="wl-bar"><div class="wl-bar-fill" style="width:${ratio}%"></div></div></div>
      <div class="wl-box losses"><div class="wl-number">${losses}</div><div class="wl-label">Derrotas</div><div class="wl-bar"><div class="wl-bar-fill" style="width:${lossRatio}%;background:linear-gradient(90deg,#e05555,#c03030)"></div></div></div>
      <div class="wl-box ratio"><div class="wl-number">${ratio}%</div><div class="wl-label">Win Rate</div><div class="wl-bar"><div class="wl-bar-fill" style="width:${ratio}%;background:linear-gradient(90deg,var(--blue-bright),var(--blue))"></div></div></div>
    </div>
    ${matches.length > 0 ? `<div class="matches-list">${matches.map(m => `
      <div class="match-row">
        <div class="match-badge ${m.wl}">${m.wl === 'w' ? 'W' : 'L'}</div>
        <div class="match-label">${m.wl === 'w' ? 'Vitória' : 'Derrota'}</div>
      </div>`).join('')}</div>` : '<p style="color:var(--grey);font-size:0.9rem;margin-top:12px;">Sem partidas registadas.</p>'}
  `;
}

function renderAdminMatches() {
  const list = document.getElementById('a-pt-matches');
  if (!list) return;
  const matches = state.matches.pt;
  list.innerHTML = matches.length > 0 ? matches.map((m, i) => `
    <div class="player-item">
      <span class="role-tag" style="background:${m.wl==='w'?'rgba(34,197,94,0.15)':'rgba(224,85,85,0.15)'};color:${m.wl==='w'?'#22c55e':'#e05555'};border:1px solid ${m.wl==='w'?'rgba(34,197,94,0.3)':'rgba(224,85,85,0.3)'}">${m.wl==='w'?'W':'L'}</span>
      <span>${m.wl === 'w' ? 'Vitória' : 'Derrota'}</span>
      <button class="del-btn" onclick="removeMatch(${i})">✕</button>
    </div>`).join('') : '<p style="color:var(--grey);font-size:0.9rem;">Sem partidas.</p>';
}

function selectWL(team, val, btn) {
  document.getElementById(team + '-wl-group').querySelectorAll('.wl-btn').forEach(b => b.classList.remove('selected'));
  btn.classList.add('selected');
  document.getElementById(team + '-add-wl').value = val;
}

async function addMatch(team) {
  const wl = document.getElementById(team + '-add-wl').value;
  if (!wl) return;
  state.matches[team].push({ wl });
  await sharedSet('arl_matches_' + team, JSON.stringify(state.matches[team]));
  document.getElementById(team + '-wl-group').querySelectorAll('.wl-btn').forEach(b => b.classList.remove('selected'));
  document.getElementById(team + '-add-wl').value = '';
  renderAdminMatches(); renderMatches();
  // Update PT stats display
  const wins = state.matches.pt.filter(m => m.wl === 'w').length;
  const losses = state.matches.pt.filter(m => m.wl === 'l').length;
  const s3 = document.getElementById('pt-stat3'); const s4 = document.getElementById('pt-stat4');
  if(s3) s3.textContent = wins; if(s4) s4.textContent = losses;
  showSaved();
}

async function removeMatch(index) {
  state.matches.pt.splice(index, 1);
  await sharedSet('arl_matches_pt', JSON.stringify(state.matches.pt));
  renderAdminMatches(); renderMatches(); showSaved();
}

function openAdmin() {
  document.getElementById('admin-overlay').classList.add('open');
  document.getElementById('admin-login').style.display = 'block';
  document.getElementById('admin-panel').style.display = 'none';
  document.getElementById('login-user').value = '';
  document.getElementById('login-pass').value = '';
  document.getElementById('login-error').style.display = 'none';
}
function closeAdmin() { document.getElementById('admin-overlay').classList.remove('open'); }
document.getElementById('admin-overlay').addEventListener('click', function(e) { if (e.target === this) closeAdmin(); });

function doLogin() {
  const u = document.getElementById('login-user').value;
  const p = document.getElementById('login-pass').value;
  if (u === 'admin' && p === 'arl2026') {
    document.getElementById('admin-login').style.display = 'none';
    document.getElementById('admin-panel').style.display = 'block';
    loadAdminFields(); renderAdminMatches();
  } else {
    document.getElementById('login-error').style.display = 'block';
  }
}

function switchTab(id, btn) {
  document.querySelectorAll('.admin-section').forEach(s => s.classList.remove('active'));
  document.querySelectorAll('.admin-tab').forEach(b => b.classList.remove('active'));
  document.getElementById(id).classList.add('active');
  btn.classList.add('active');
}

function loadAdminFields() {
  ALL_FIELDS.forEach(([adminId, pageId]) => {
    const admin = document.getElementById(adminId);
    const page = document.getElementById(pageId);
    if (admin && page) admin.value = page.textContent;
  });
}

async function syncAndSave(pairs) {
  for (const [adminId, pageId] of pairs) {
    const admin = document.getElementById(adminId);
    const page = document.getElementById(pageId);
    if (admin && page && admin.value.trim() !== '') {
      page.textContent = admin.value.trim();
      await sharedSet('arl_' + pageId, admin.value.trim());
    }
  }
}

async function saveHomeContent() {
  await syncAndSave([
    ['a-home-subtitle','home-subtitle'],['a-home-desc','home-desc'],
    ['a-about-title','about-title'],['a-about-text','about-text'],
    ['a-val1-title','val1-title'],['a-val1-text','val1-text'],
    ['a-val2-title','val2-title'],['a-val2-text','val2-text'],
    ['a-val3-title','val3-title'],['a-val3-text','val3-text'],
  ]);
  showSaved();
}
async function saveRLContent() {
  await syncAndSave([
    ['a-rl-subtitle','rl-subtitle'],['a-rl-about-title','rl-about-title'],['a-rl-about-text','rl-about-text'],
    ['a-rl-stat1','rl-stat1'],['a-rl-stat2','rl-stat2'],['a-rl-stat3','rl-stat3'],['a-rl-stat4','rl-stat4'],
  ]);
  showSaved();
}
async function savePTContent() {
  await syncAndSave([
    ['a-pt-subtitle','pt-subtitle'],['a-pt-about-title','pt-about-title'],['a-pt-about-text','pt-about-text'],
    ['a-pt-stat1','pt-stat1'],['a-pt-stat2','pt-stat2'],['a-pt-stat3','pt-stat3'],['a-pt-stat4','pt-stat4'],
  ]);
  showSaved();
}
async function saveStats() {
  await syncAndSave([
    ['a-stat-members','stat-members'],['a-stat-players','stat-players'],
    ['a-stat-tournaments','stat-tournaments'],['a-stat-wins','stat-wins'],
  ]);
  showSaved();
}

function showSaved() {
  const ind = document.getElementById('save-indicator');
  ind.classList.add('show');
  setTimeout(() => ind.classList.remove('show'), 2500);
}

loadFromStorage();
</script>
</body>
</html>
